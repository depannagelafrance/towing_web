<label>Facturatiegegevens:</label>
<div id="edit-invoice-data" class="facturation-container__info">

{{#if company_name}}
<div class="has_content">
{{else}}
{{#if last_name}}
<div class="has_content">
{{/if}}
{{/if}}

    {{#if company_name}}
        <div>{{company_name}}</div>
    {{else}}
        {{#if last_name}}
            {{#if first_name}}
                <div>{{first_name}} {{last_name}}</div>
            {{/if}}
        {{/if}}
    {{/if}}

    {{#if street}}
        {{#if street_number}}
            <div>{{street}} {{street_number}} {{street_pobox}}</div>
        {{/if}}
    {{/if}}

    {{#if city}}
        {{#if zip}}
            <div>{{zip}} {{city}}</div>
        {{/if}}
    {{/if}}

    {{#if country}}
        <div>{{country}} </div>
    {{/if}}

    {{#if phone}}
        <div>T: {{phone}} </div>
    {{/if}}

    {{#if email}}
        <div>E: {{email}} </div>
    {{/if}}

{{#if company_name}}
</div>
{{else}}
{{#if last_name}}
</div>
{{/if}}
{{/if}}

                </div>
<a id="edit-invoice-data-link" class="inform-link icon--edit--small" href="#customer_form">Bewerken</a>
